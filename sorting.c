#include<stdio.h>
int add(int a,int b){
   int add=a+b;
}
int sum (int p,int q);
int main(){
    printf("Enter the value of p and q:");
    // printf("Enter the value of p and q:");
    scanf("%d %d",&p,&q);
    sum(p,q);
    printf("the sum is %d",sum);
    return 0;

}