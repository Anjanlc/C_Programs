#include<stdio.h>
int main ()
{
    int number, temporary_number, sum=0, number_digit=0, digit=0;

    printf("Enter a number to check wheather it is armstrong or not :");
    scanf("%d",&number);
    
}