#include<stdio.h>
int main()
{
    char value;
    printf("Enter a character to find its ascii value:");
    scanf("%c",&value);
    printf("The ascii value of %c is %d",value,value);
    return 0;
}
